
<!-- ADD THESE BUTTONS -->
<Button
    android:id="@+id/startLogBtn"
    android:text="Start LOG"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"/>

<Button
    android:id="@+id/stopLogBtn"
    android:text="Stop LOG"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"/>
